name: kataho_code_example
description: Example app for the kataho_code package.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  kataho_code:
    path: ..

flutter:
  uses-material-design: true
